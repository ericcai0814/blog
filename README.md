# blog

Technical blog - learning notes on frontend, Vue.js, and AI-assisted development

Production deploys `next-site/` through the root `Dockerfile` so Zeabur can keep
its service root at `/` while running the Next.js rewrite.
